[build]
buildCommand = "pip install -r requirements.txt && python manage.py collectstatic --noinput"

[deploy]
startCommand = "sh -c 'python manage.py migrate && gunicorn core.wsgi --bind 0.0.0.0:$PORT --log-file -'"